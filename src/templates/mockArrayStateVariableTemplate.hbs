function set_{{setFunction.functionName}}({{setFunction.paramType}} memory _{{setFunction.paramName}}) public {
  {{setFunction.paramName}} = _{{setFunction.paramName}};
}

function mock_call_{{mockFunction.functionName}}({{mockFunction.paramType}} memory _{{mockFunction.functionName}}) public {
  vm.mockCall(
    address(this),
    abi.encodeWithSelector(I{{mockFunction.contractName}}.{{mockFunction.functionName}}.selector),
    abi.encode(_{{mockFunction.functionName}})
  );
}
{{nl}}
