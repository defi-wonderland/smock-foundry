function set_{{setFunction.functionName}}({{setFunction.keyType}} _key, {{setFunction.valueType}} _value) public {
  {{setFunction.mappingName}}[_key] = _value;
}

{{#unless isInternal}}
function mock_call_{{mockFunction.functionName}}({{mockFunction.keyType}} _key, {{mockFunction.valueType}} _value) public {
  vm.mockCall(
    address(this),
    abi.encodeWithSignature("{{mockFunction.functionName}}({{mockFunction.keyType}})", _key),
    abi.encode(_value)
  );
}
{{/unless}}
{{nl}}
